# 🚀 Арифметический Сервис с Очередью (Arithmetic Progression Service)

Сервис, реализованный на **Go**, предназначен для асинхронного вычисления арифметических прогрессий. Задачи поступают в общую очередь и параллельно выполняются ограниченным числом обработчиков ($N$).

## ✨ Основные особенности

* **Асинхронная очередь:** Задачи ставятся в очередь через HTTP-endpoint.
* **Параллельное выполнение:** Ограниченное количество ($N$) горутин-обработчиков (воркеров) выполняет задачи параллельно. $N$ задается при запуске сервиса.
* **Вычисление по интервалу:** Каждый шаг прогрессии ($N_k = N_{k-1} + D$) выполняется только после наступления заданного интервала $I$, отсчитываемого от времени выполнения предыдущего шага.
* **In-Memory хранение:** Все данные и состояние задач хранятся в памяти, обеспечивая быстрый доступ.
* **TTL-удаление:** Завершенные задачи помечаются как истекшие (Expired) после прохождения заданного времени жизни (TTL).
* **Docker-контейнер:** Упаковка для легкого развертывания.

---

## ⚙️ Технологии

* **Язык:** Go (Golang)
* **Параллелизм:** Горутины и Каналы (Worker Pool Pattern)
* **Хранение:** In-Memory (`sync.RWMutex` для безопасного доступа)
* **Развертывание:** Docker

---

## 🐳 Запуск проекта с помощью Docker

Для запуска сервиса вам потребуется установленный Docker.

### 1. Сборка Docker-образа

Выполните команду в корневой директории проекта:

```bash
docker build -t arithmetic-service .
